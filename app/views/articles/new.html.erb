<h1>Create a new article</h1>
<%= form_for(@article) do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title %>

  <%= a.label :content %>
  <%= a.text_field :content %>

  <%= a.submit class: "btn btn-default" %>
<% end %>
